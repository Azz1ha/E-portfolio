{% extends 'base.html.twig' %}

{% block title %}CV - Hala Azzi{% endblock %}

{% block body %}
<style>
    body {
        background: linear-gradient(135deg, #ff69b4 0%, #ffb6d9 100%);
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        margin: 0;
        padding: 0;
    }

    .navbar {
        background-color: #ff1493 !important;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
    }

    .navbar-brand {
        font-weight: bold;
        font-size: 1.5em;
    }

    .nav-link {
        color: white !important;
        font-weight: 500;
        transition: all 0.3s ease;
        position: relative;
    }

    .nav-link:hover {
        color: #ffe0f0 !important;
        transform: translateX(5px);
        text-shadow: 0 0 10px rgba(255, 224, 240, 0.8), 0 0 20px rgba(255, 224, 240, 0.6);
    }

    .dropdown-menu {
        background-color: #ff1493 !important;
        border: none;
    }

    .dropdown-item {
        color: white !important;
        transition: background-color 0.3s ease;
    }

    .dropdown-item:hover {
        background-color: #ff69b4 !important;
    }

    footer {
        background-color: #ff1493 !important;
        color: white !important;
        margin-top: auto;
    }

    footer a {
        color: #ffe0f0 !important;
        text-decoration: none;
    }

    footer a:hover {
        color: white !important;
        text-decoration: underline;
    }

    /* CV specific styles */
    .cv-container {
        max-width: 1000px;
        margin: 0 auto;
        padding: 40px 20px;
    }

    .cv-header {
        text-align: center;
        margin-bottom: 60px;
        padding: 40px;
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        border-radius: 20px;
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
        border: 1px solid rgba(255, 255, 255, 0.1);
    }

    .cv-title {
        font-size: 3.5em;
        font-weight: bold;
        color: white;
        margin-bottom: 20px;
        text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
    }

    .cv-subtitle {
        font-size: 1.8em;
        color: #ffe0f0;
        font-style: italic;
        margin-bottom: 20px;
    }

    .contact-info {
        display: flex;
        flex-direction: column;
        gap: 10px;
        text-align: center;
    }

    .contact-info p {
        margin: 0;
        font-size: 1.1em;
    }

    .contact-info i {
        margin-right: 10px;
        width: 20px;
    }

    .cv-section {
        margin-bottom: 50px;
        padding: 30px;
        background: rgba(255, 255, 255, 0.08);
        backdrop-filter: blur(15px);
        border-radius: 15px;
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
        border: 1px solid rgba(255, 255, 255, 0.1);
        transition: transform 0.3s ease;
    }

    .cv-section:hover {
        transform: translateY(-5px);
    }

    .cv-section-title {
        font-size: 2.2em;
        font-weight: bold;
        color: white;
        margin-bottom: 30px;
        text-align: center;
        position: relative;
    }

    .cv-section-title::after {
        content: '';
        position: absolute;
        bottom: -10px;
        left: 50%;
        transform: translateX(-50%);
        width: 100px;
        height: 3px;
        background: linear-gradient(90deg, transparent, #ff69b4, transparent);
        border-radius: 2px;
    }

    .cv-item {
        margin-bottom: 25px;
        padding: 20px;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 10px;
        border-left: 4px solid #ff69b4;
        transition: all 0.3s ease;
    }

    .cv-item:hover {
        background: rgba(255, 255, 255, 0.1);
        transform: translateX(10px);
    }

    .cv-item-title {
        font-size: 1.4em;
        font-weight: bold;
        color: white;
        margin-bottom: 8px;
    }

    .cv-item-subtitle {
        font-size: 1.1em;
        color: #ffb6d9;
        font-weight: 500;
        margin-bottom: 10px;
    }

    .cv-item-date {
        font-size: 0.9em;
        color: #ffe0f0;
        font-style: italic;
        margin-bottom: 15px;
    }

    .cv-item-description {
        color: #e0e0e0;
        line-height: 1.6;
        margin: 0;
    }

    .competence-list {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 20px;
        margin-top: 20px;
    }

    .competence-item {
        padding: 15px;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 10px;
        text-align: center;
        transition: all 0.3s ease;
        border: 1px solid rgba(255, 255, 255, 0.1);
    }

    .competence-item:hover {
        background: rgba(255, 255, 255, 0.1);
        transform: scale(1.05);
    }

    .competence-item i {
        font-size: 2em;
        color: #ff69b4;
        margin-bottom: 10px;
        display: block;
    }

    .competence-item h5 {
        color: white;
        font-size: 1.1em;
        margin-bottom: 8px;
        font-weight: bold;
    }

    .competence-item p {
        color: #ffe0f0;
        font-size: 0.9em;
        margin: 0;
        line-height: 1.4;
    }

    .back-button {
        display: inline-block;
        margin-top: 40px;
        padding: 12px 30px;
        background: linear-gradient(135deg, #ff69b4, #ffb6d9);
        color: white;
        text-decoration: none;
        border-radius: 25px;
        font-weight: bold;
        transition: all 0.3s ease;
        box-shadow: 0 4px 15px rgba(255, 105, 180, 0.3);
    }

    .back-button:hover {
        transform: translateY(-3px);
        box-shadow: 0 8px 25px rgba(255, 105, 180, 0.4);
        color: white;
        text-decoration: none;
    }

    /* Dark mode styles */
    body.dark-mode .cv-header {
        background: rgba(74, 28, 58, 0.4);
    }

    body.dark-mode .cv-section {
        background: rgba(255, 255, 255, 0.05);
    }

    body.dark-mode .cv-item {
        background: rgba(255, 255, 255, 0.03);
    }

    body.dark-mode .competence-item {
        background: rgba(255, 255, 255, 0.03);
    }

    body.dark-mode footer {
        background-color: #2d0f26 !important;
    }

    body.dark-mode footer a {
        color: #e0e0e0 !important;
    }

    body.dark-mode footer a:hover {
        color: white !important;
    }

    /* Responsive design */
    @media (max-width: 768px) {
        .cv-title {
            font-size: 2.5em;
        }

        .cv-subtitle {
            font-size: 1.4em;
        }

        .cv-section-title {
            font-size: 1.8em;
        }

        .competence-list {
            grid-template-columns: 1fr;
        }
    }
</style>

<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">E-portfolio</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_Her') }}#top">Accueil</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Compétences</a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('app_competence_administrer') }}">Administrer</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_competence_connecter') }}">Connecter</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_competence_programmer') }}">Programmer</a></li>
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Projets</a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('app_projet_1') }}">Projet 1</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_projet_2') }}">Projet 2</a></li>
                    </ul>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_Her') }}#interets">Intérêts</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_inscription_cv') }}">Télécharger CV</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_Her') }}#portfolio">Portfolio</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_Her') }}#contact">Contact</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_Her') }}#about">À propos</a>
                </li>
            </ul>
            <button id="theme-toggle" class="btn btn-outline-light ms-3" title="Basculer le thème">
                <i class="fas fa-moon"></i>
            </button>
        </div>
    </div>
</nav>

<div class="cv-container">
    <!-- CV Header -->
    <div class="cv-header">
        <h1 class="cv-title">Hala Azzi</h1>
        <p class="cv-subtitle">Ingénieure Réseaux et Télécommunications</p>
        <div class="contact-info" style="margin-top: 20px; color: #ffe0f0;">
            <p><i class="fas fa-envelope"></i> hala.azzi@email.com</p>
            <p><i class="fas fa-phone"></i> +33 6 XX XX XX XX</p>
            <p><i class="fas fa-map-marker-alt"></i> Ville, Pays</p>
        </div>
    </div>

    <!-- Formations Section -->
    <div class="cv-section">
        <h2 class="cv-section-title">Formations</h2>

        <div class="cv-item">
            <h3 class="cv-item-title">Master Réseaux et Télécommunications</h3>
            <p class="cv-item-subtitle">Université X, Ville</p>
            <p class="cv-item-date">2028 - 2030</p>
            <p class="cv-item-description">
                Spécialisation en architectures réseau, sécurité informatique et technologies de communication.
                Projet de fin d'études : Conception d'un réseau d'entreprise sécurisé.
            </p>
        </div>

        <div class="cv-item">
            <h3 class="cv-item-title">BUT Réseaux et Télécommunications</h3>
            <p class="cv-item-subtitle">IUT de Roanne, Roanne</p>
            <p class="cv-item-date">2025 - 2028</p>
            <p class="cv-item-description">
                Formation complète en réseaux informatiques, télécommunications et administration système.
            </p>
        </div>
    </div>

    <!-- Expériences Professionnelles Section -->
    <div class="cv-section">
        <h2 class="cv-section-title">Expériences Professionnelles</h2>

        <div class="cv-item">
            <h3 class="cv-item-title">Stagiaire Ingénieure Réseaux</h3>
            <p class="cv-item-subtitle">Entreprise A, Ville</p>
            <p class="cv-item-date">Mars 2025 - Août 2025</p>
            <p class="cv-item-description">
                Configuration et maintenance d'infrastructures réseau. Mise en place de solutions de sécurité,
                déploiement de serveurs et optimisation des performances réseau. Participation à des projets
                de migration vers le cloud.
            </p>
        </div>
    </div>

    <!-- Compétences Section -->
    <div class="cv-section">
        <h2 class="cv-section-title">Compétences</h2>

        <div class="competence-list">
            <div class="competence-item">
                <i class="fas fa-network-wired"></i>
                <h5>Réseaux</h5>
                <p>Cisco, TCP/IP, VLAN</p>
            </div>

            <div class="competence-item">
                <i class="fas fa-code"></i>
                <h5>Programmation</h5>
                <p>Python, JavaScript, C</p>
            </div>

            <div class="competence-item">
                <i class="fas fa-server"></i>
                <h5>Systèmes</h5>
                <p>Linux, Windows Server</p>
            </div>
        </div>
    </div>

    <div style="text-align: center;">
        <a href="{{ path('app_Her') }}" class="back-button">
            <i class="fas fa-arrow-left"></i> Retour à l'accueil
        </a>
    </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const themeToggle = document.getElementById('theme-toggle');
    const body = document.body;
    const icon = themeToggle.querySelector('i');

    // Vérifier le thème sauvegardé
    const savedTheme = localStorage.getItem('theme');
    if (savedTheme === 'dark') {
        body.classList.add('dark-mode');
        icon.classList.remove('fa-moon');
        icon.classList.add('fa-sun');
    }

    // Basculer le thème
    themeToggle.addEventListener('click', function() {
        body.classList.toggle('dark-mode');

        if (body.classList.contains('dark-mode')) {
            localStorage.setItem('theme', 'dark');
            icon.classList.remove('fa-moon');
            icon.classList.add('fa-sun');
        } else {
            localStorage.setItem('theme', 'light');
            icon.classList.remove('fa-sun');
            icon.classList.add('fa-moon');
        }
    });
});
</script>

{% endblock %}